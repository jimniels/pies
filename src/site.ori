{
  // Feed data pulled from archive on CDN
  (_feed) = https://cdn.jim-nielsen.com/pies/feed.json

  // Website
  index.html = index.ori.html(_feed)
  feed.json = Tree.json({
    ..._feed,
    items: Tree.take(_feed.items, 10),
  })
  ...(../static)
}
